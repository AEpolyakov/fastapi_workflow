services:
  fw:
    env_file:
      - .env
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port ${APP_PORT} --log-level ${LOG_LEVEL}
    ports:
      - "${APP_PORT}:${APP_PORT}"
    environment:
      - PYTHONUNBUFFERED=1
